/**
 * Write a function which sets the first, third, fifth, etc number of the given array to 0,
 * but leaves the second, fourth, etc numbers unchanged
 * 
 * e.g. ([4, 2, 9, 3]) => [0, 2, 0, 3]
 */
function flattenOdds (arr) {
  // code here
}

/**
 * Write a function which accepts an array, and returns a new array whose first
 * entry is equal to the sum of the first two numbers, whose second entry is equal
 * to the sum of the next two numbers, and so on...
 * 
 * e.g. ([4, 2, 9, 3]) => [6, 12]
 */
function addPairs (arr) {
  // code here
}

/**
 * Write a function which goes through the array and multiplies together each group of three entries.
 * Return the sum of all the multiples you calculated.
 * 
 * e.g. ([2, 4, 5, 7, 1, 6]) => 2*4*5 + 7*1*6 = 40 + 42 = 82
 */
function triplets (arr) {
  // code here
}