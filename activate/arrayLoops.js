/**
 * Write a function which returns the sum of the numbers in the given array,
 * e.g. ([1, 4, 6]) => 11
 */
function addArray(arr) {
  // code here
}

/**
 * Write a function which return the sum of only the even numbers in the given array,
 * e.g. ([1, 4, 6]) => 10
 */
function addEvens(arr) {
  // code here
}

/**
 * Write a function which counts how many even numbers there are in the given array,
 * e.g. ([1, 4, 6]) => 2
 */
function countEvens(arr) {
  // code here
}

/**
 * Write a function which returns a new array where each number has been multiplied by its index position,
 * e.g. ([1, 4, 6]) => [0, 4, 12]
 */
function filterEvens(arr) {
  // code here
}
