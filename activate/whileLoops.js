/**
 * Write a function which returns the largest power of 2 which is smaller than the limit
 * e.g. (19) => 16
 * because 2**4 = 16 but 2**5 = 32 is too large
 */
function powerOf2 (limit) {
  // code here
}

/**
 * Write a function which returns the largest square number smaller than the limit
 * e.g. (30) => 25
 * because 5**2 = 25 but 6**2 = 36 is too large
 */
function squareLimit (limit) {
  // code here
}

/**
 * Write a function which finds the largest sum of consecutive integers smaller than the limit
 * e.g. (8) => 6
 * because 1 + 2 + 3 = 6 but 1 + 2 + 3 + 4 = 10 is too large
 */
function sumLimit (limit) {
  // code here
}

/**
 * Write a function which repeats the given string as many times as possible
 * without exceeding the given limit.
 * e.g. ('abc', 10) => 'abcabcabc'
 */
function repeatLimit (str, limit) {
  // code here
}
