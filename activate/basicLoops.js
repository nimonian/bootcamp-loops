/**
 * Adds up all the numbers from 1 to n and returns the answer
 */
function addNums(n) {
  let answer = 0

  for (let i = 1; i <= n; i += 1) {
    answer += i
  }

  return answer
}

/**
 * Write a function that adds up all the numbers from a to b and returns the answer
 */
function addNums2(a, b) {
  // code here
}

/**
 * Write a function that adds up all the even numbers from a to b and returns the answer
 */
function addEvens(a, b) {
  // code here
}

/**
 * Write a function that creates a string with the numbers from 1 to n concatenated,
 * e.g. (10) => '12345678910'
 */
function numberString(n) {
  // code here
}
